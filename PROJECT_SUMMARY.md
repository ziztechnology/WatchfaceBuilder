# WatchfaceBuilder Project Summary

## 项目概述

WatchfaceBuilder 是一个独立的开源项目，用于快速生成 H5 表盘包。它从 tbe_go 后端项目中分离出来，专注于表盘构建功能。

## 项目结构

```
WatchfaceBuilder/
├── .github/
│   └── workflows/          # GitHub Actions 自动化
│       ├── ci.yml          # 持续集成
│       └── release.yml     # 自动发布
├── cmd/
│   └── cli/                # CLI 命令行工具
│       └── main.go         # 主程序入口
├── pkg/
│   └── builder/            # 核心构建逻辑
│       ├── builder.go      # 主要构建器
│       └── templates.go    # 内置模板
├── docs/                   # 文档
│   ├── API.md             # API 文档（预留）
│   └── QUICKSTART.md      # 快速开始指南
├── examples/               # 示例项目
│   ├── README.md
│   └── simple/
│       └── build.sh
├── .gitignore
├── CONTRIBUTING.md         # 贡献指南
├── LICENSE                 # MIT 许可证
├── Makefile               # 构建脚本
├── README.md              # 英文文档
├── README_zh.md           # 中文文档
├── go.mod                 # Go 模块定义
└── go.sum                 # 依赖锁定
```

## 核心功能

### 1. CLI 工具
- **交互式模式** (`-i`): 通过问答方式创建表盘
- **命令行模式**: 通过参数快速创建
- **模板列表** (`-list`): 查看可用模板

### 2. 内置模板

#### Simple (简约数字时钟)
- 大字号数字时间显示 (HH:MM:SS)
- 日期显示
- 渐变背景 (紫蓝色渐变)
- 响应式设计

#### Analog (指针时钟)
- Canvas 绘制的指针时钟
- 时针、分针、秒针
- 12 个时刻度
- 流畅的动画效果
- 红色秒针，黑色/灰色时分针

#### Digital (数字显示时钟)
- 大号数字时分秒显示
- 带闪烁效果的分隔符
- 日期和星期显示
- 霓虹灯效果（青色发光）
- 深色科技背景

#### Custom (自定义模板)
- 支持自定义 HTML/CSS/JS
- 完全控制表盘外观和行为

### 3. 自动生成功能
- **manifest.json**: 表盘元数据
- **preview.png**: 512x512 预览图
- **ZIP 打包**: 自动打包所有文件
- **文件哈希**: SHA256 校验和

## 技术栈

- **语言**: Go 1.21+
- **CLI 框架**: cobra (命令行参数解析)
- **图像生成**: fogleman/gg (预览图生成)
- **打包**: archive/zip (标准库)

## 依赖项

```go
require (
    github.com/fogleman/gg v1.3.0
    github.com/spf13/cobra v1.8.0
)
```

## 使用示例

### 交互式模式
```bash
./watchface-builder -i
```

### 快速创建
```bash
./watchface-builder --name "我的表盘" --template analog
```

### 完整参数
```bash
./watchface-builder \
  --name "我的表盘" \
  --template digital \
  --version 1.0.0 \
  --author "你的名字" \
  --description "一个漂亮的表盘" \
  --tags "科技,数字" \
  --output ./output
```

### 自定义模板
```bash
./watchface-builder \
  --name "自定义表盘" \
  --template custom \
  --custom-html-file my.html \
  --custom-css-file my.css \
  --custom-js-file my.js
```

## 生成的包结构

```
watchface.zip
├── manifest.json       # 表盘元数据
├── index.html          # HTML 入口文件
├── style.css           # CSS 样式
├── script.js           # JavaScript 逻辑
└── preview.png         # 预览图（可选）
```

### manifest.json 示例

```json
{
  "name": "我的表盘",
  "version": "1.0.0",
  "author": "你的名字",
  "description": "一个漂亮的表盘",
  "entrypoint": "index.html",
  "tags": ["简约", "数字"],
  "created_at": "2025-01-21T10:30:00Z"
}
```

## 构建命令

### 开发构建
```bash
make build
```

### 跨平台构建
```bash
make build-all
```

生成文件：
- `build/watchface-builder-linux-amd64`
- `build/watchface-builder-darwin-amd64`
- `build/watchface-builder-darwin-arm64`
- `build/watchface-builder-windows-amd64.exe`

### 发布版本
```bash
make release
```

生成发布包：
- `build/watchface-builder-linux-amd64.tar.gz`
- `build/watchface-builder-darwin-amd64.tar.gz`
- `build/watchface-builder-darwin-arm64.tar.gz`
- `build/watchface-builder-windows-amd64.zip`

## GitHub Actions

### CI 工作流 (.github/workflows/ci.yml)
- 在 Linux、macOS、Windows 上测试
- 运行所有测试用例
- 代码检查 (golangci-lint)

### Release 工作流 (.github/workflows/release.yml)
- 推送 tag 时自动触发
- 构建所有平台的二进制文件
- 生成 SHA256 校验和
- 创建 GitHub Release
- 上传所有构建产物

## 发布流程

1. **更新版本号**:
   ```bash
   # 在 cmd/cli/main.go 中更新版本常量
   ```

2. **创建 tag**:
   ```bash
   git tag -a v0.2.0 -m "Release v0.2.0 - 新功能描述"
   git push origin v0.2.0
   ```

3. **自动构建**: GitHub Actions 自动构建并创建 Release

4. **发布说明**: 在 GitHub Release 页面完善发布说明

## 与 tbe_go 项目的集成

### 方式1: CLI 工具调用
用户可以下载 WatchfaceBuilder CLI 工具，本地生成表盘包，然后上传到 tbe_go 后端进行校验。

### 方式2: Web API 集成（未来）
可以将 WatchfaceBuilder 作为微服务运行，tbe_go 通过 API 调用生成表盘包。

### 方式3: SDK 集成（未来）
将 `pkg/builder` 作为库导入到 tbe_go 项目中。

## 优势

1. **独立性**: 用户无需部署整个后端即可使用构建器
2. **开源**: 社区可以贡献模板和功能
3. **跨平台**: 支持 Windows、macOS、Linux
4. **易用性**: 交互式模式适合新手
5. **可扩展**: 易于添加新模板
6. **文档完善**: 英文和中文双语文档

## 路线图

### v0.1.0 (当前版本)
- ✅ CLI 工具
- ✅ 3 个内置模板
- ✅ 自定义模板支持
- ✅ 预览图生成
- ✅ 跨平台构建

### v0.2.0 (计划中)
- [ ] Web UI 界面
- [ ] 更多内置模板
- [ ] 实时预览
- [ ] 模板编辑器

### v0.3.0 (计划中)
- [ ] Web API 服务
- [ ] 模板市场
- [ ] 云端构建

### v1.0.0 (长期目标)
- [ ] GUI 桌面应用
- [ ] 插件系统
- [ ] 模板商店

## 许可证

MIT License - 开源免费使用

## 贡献

欢迎贡献！请阅读 [CONTRIBUTING.md](CONTRIBUTING.md)

## 支持

- 🐛 [报告问题](https://github.com/ziztechnology/WatchfaceBuilder/issues)
- 💬 [讨论](https://github.com/ziztechnology/WatchfaceBuilder/discussions)
- 📖 [文档](docs/)

---

**当前状态**: ✅ 项目已完成基础功能，可以开源发布

**初始提交**: 5fa746d

**版本标签**: v0.1.0
